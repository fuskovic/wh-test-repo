apiVersion: v1
kind: Secret
# github
# metadata:
#   name: gh-wh-secret
#   namespace: kargo-demo
# stringData:
#   token: my-not-so-secret-token
#
# quay
metadata:
  name: quay-wh-secret
  namespace: kargo-demo
stringData:
  quay-secret: my-insecure-secret